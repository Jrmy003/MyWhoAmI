<Page
    x:Class="WhoAmI.UWP.views.NewCharacterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WhoAmI.UWP.views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Path=CharacterViewModel, Source={StaticResource ViewModelLocator}}">

    <StackPanel Background="Black">
        <TextBlock Text="Firstname" Foreground="WhiteSmoke" FontSize="26"/>
        <TextBox Text="{Binding Firstname}" Name="tbFirstname" Foreground="WhiteSmoke" FontSize="26"/>
        <TextBlock Text="Lastname" Foreground="WhiteSmoke" FontSize="26"/>
        <TextBox Text="{Binding Lastname}" Name="tbLastname" Foreground="WhiteSmoke" FontSize="26"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Command="{Binding SaveCharacterCommand}" Style="{StaticResource WhiteSmokeButton}">Save Character</Button>
            <Button Style="{StaticResource WhiteSmokeButton}">Reset</Button>
        </StackPanel>
    </StackPanel>
</Page>
